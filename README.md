1. Initialised a Turborepo
2. Deleted the Docs app
3. Created two backend apps , one for http and another for websockets
4. Added Package.json by initialising npm init -y
5. Initialised ts config files and extends it from base.json present in the global typescript-config package
6. Addes "@repo/typescript-config": "workspace:*" dependency in package.json in both the backends